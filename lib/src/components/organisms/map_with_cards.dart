import 'package:flutter/material.dart';

class MapWithCards extends StatelessWidget {
  const MapWithCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 300,
      color: Colors.white,
      strokeWidth: 2,
    );
  }
}
